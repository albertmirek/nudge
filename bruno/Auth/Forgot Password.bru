meta {
  name: Forgot Password
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/v1/auth/forgot-password
  body: json
}

body:json {
  {
    "email": "{{email}}"
  }
}

docs {
  Sends a password reset code email (always returns 204, even if email not found).
}

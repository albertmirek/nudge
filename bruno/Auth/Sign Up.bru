meta {
  name: Sign Up
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/auth/sign-up
  body: json
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}",
    "timezone": "Europe/Prague"
  }
}

docs {
  Creates a new account and sends a verification code email.
}

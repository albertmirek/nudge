# PostgreSQL (used by the db container)
POSTGRES_USER=nudge
POSTGRES_PASSWORD=nudge
POSTGRES_DB=nudge

# NestJS server
PORT=3000
# Inside docker compose the host is `db`; when running the server natively use localhost.
DATABASE_URL=postgresql://nudge:nudge@db:5432/nudge
